<div class="furflile" id="furfile">
  {{#each this}}
  <section class="furfile">
    <div class="meta">
      {{user.username}} 
       {{format_date created_at}}
    </div>
    <div class="text">
      {{furfile_text}}
    </div>
  </section>
  {{/each}}
</div>